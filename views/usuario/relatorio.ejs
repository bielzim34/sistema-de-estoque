<!DOCTYPE html>
<html lang="en"data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Relatório dos usuarios</title>
</head>
<body>

    <%- include("../partials/cabecalho.ejs") %>

    <div class="container">
        <% if (usuarios) { %>

            <table class="table table-striped">
                <thead>
                    
                        <th>Nome</th>
                        <th>E-mail</th>
                        <th>Detalhar</th>
                        <th>Atualizar</th>
                        <th>Excluir</th>
                    
                </thead>

                <% usuarios.forEach(usuario => { %>
                    <tr class="table-info">
                        <td><%= usuario.nome %></td>
                        <td><%= usuario.email%></td>
                        
                        <td><a href="/usuarios/<%= usuario.email %>" class="btn btn-info"> Detalhar</a></td>
                        <td><a href="/usuarios/atualizar/<%= usuario.email%>" class="btn btn-warning"> Atualizar</a></td>
                        <td><a href="/usuarios/excluir/<%= usuario.email%>" class="btn btn-danger" onclick="return confirm('Deseja excluir?')">Deletar</a></td>
                        </tr>

                        <%}) %>
            </table>
            
            
        
            <%} else{ %>
                <h2 style="color: red;"> Usuario NÃO cadastrado!
                    <%} %>

                    
                <% if (status ==1 ) { %>
                    <div class="alert alert-success">
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        Cadastrado com sucesso.
                        
                    
                    </div>
                    
                <%} %>

                <% if (status==2 ) { %>
                    <div class="alert alert-danger alert-dismissible">
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        REMOVIDO COM SUCESSO.
                <%} %>

                <a href="/" class="btn btn-primary"> Voltar</a>
    </div>

          <%- include("../partials/rodape.ejs") %>
</body>
</html>